# 基础依赖
tensorflow==2.16.1      # 主框架 (CPU/GPU 按需安装)
numpy>=1.26.0           # 数值计算
pandas>=2.2.0           # 数据处理（可选）

# 配置 & 工具
pyyaml>=6.0.1           # 读取 config.yaml 配置文件
tqdm>=4.66.1            # 训练进度条

# 可视化
matplotlib>=3.9.0       # 绘制 loss / accuracy 曲线
seaborn>=0.13.0         # 数据可视化（可选）

# Notebook 环境
jupyter>=1.0.0          # 运行 notebooks/

# 
pytest>=8.0.0
